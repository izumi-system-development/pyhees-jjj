[build-system]
requires = ["setuptools>=67.8.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "pyhees-jjj"
version = "0.0.1"
description = "Pyheesの検証用のフォークプロジェクトです"
authors = [
    { name = "Pyhees Development Team" }
]
license = { file = "LICENSE" }
requires-python = ">=3.10"
dependencies = [
    "numpy>=1.24.3",
    "pandas>=2.0.1",
    "scipy>=1.10.1",
    "injector",
    "pytest>=6.0"
]

[project.urls]
homepage = "https://github.com/izumi-system-development/pyhees-jjj"
